# WRight Now - Development Environment Variables
# 
# ⚠️ SECURITY WARNING ⚠️
# These are DEVELOPMENT-ONLY credentials. They are intentionally insecure for ease of setup.
# NEVER use these values in production. Production credentials are managed via Kubernetes secrets.
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Customize values if needed (defaults work for most cases)
# 3. Run: docker-compose up -d
# 4. Services will be available at:
#    - PostgreSQL: localhost:5432
#    - Redis: localhost:6379
#    - Authentik: http://localhost:9000

# ============================================================================
# PostgreSQL (Main Application Database)
# ============================================================================
# Database name for the main WRight Now application
POSTGRES_DB=wrightnow

# PostgreSQL superuser username
POSTGRES_USER=postgres

# PostgreSQL superuser password
# ⚠️ INSECURE - Change in production
POSTGRES_PASSWORD=password

# ============================================================================
# Redis (Main Application Cache)
# ============================================================================
# Redis doesn't require a password in development for simplicity
# Uncomment to enable password authentication:
# REDIS_PASSWORD=redis_dev_password

# ============================================================================
# Authentik IdP Configuration
# ============================================================================

# Authentik PostgreSQL Database
AUTHENTIK_POSTGRES_DB=authentik
AUTHENTIK_POSTGRES_USER=authentik
# ⚠️ INSECURE - Change in production
AUTHENTIK_POSTGRES_PASSWORD=authentik

# Authentik Secret Key
# Used for cryptographic signing and should be unique per installation
# ⚠️ INSECURE - Generate a strong random key in production
# Generate with: openssl rand -base64 32
AUTHENTIK_SECRET_KEY=this-is-a-secret-key-change-in-production

# Authentik Error Reporting
# Set to true to enable error reporting to Authentik developers (optional)
AUTHENTIK_ERROR_REPORTING=false

# Authentik Log Level
# Options: debug, info, warning, error
AUTHENTIK_LOG_LEVEL=info

# Authentik Bootstrap (Optional)
# Uncomment to set an admin password on first startup
# AUTHENTIK_BOOTSTRAP_PASSWORD=admin
# AUTHENTIK_BOOTSTRAP_TOKEN=your-bootstrap-token-here

# ============================================================================
# Development Tips
# ============================================================================
#
# 1. View logs:
#    docker-compose logs -f [service-name]
#
# 2. Connect to PostgreSQL:
#    docker exec -it wrightnow-postgres psql -U postgres -d wrightnow
#
# 3. Connect to Redis:
#    docker exec -it wrightnow-redis redis-cli
#
# 4. Reset environment (⚠️ deletes all data):
#    docker-compose down -v
#
# 5. Check service health:
#    docker-compose ps
#
# ============================================================================
